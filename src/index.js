import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Vga from './Vga';

ReactDOM.render(
  <React.StrictMode>
    <Vga />
  </React.StrictMode>,
  document.getElementById('root')
);
